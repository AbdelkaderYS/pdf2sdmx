# What a host installs by default, and what a 512 MB one can carry: the text stage only.
#
# Camelot needs torch, which is 738 MB on disk before a model is even downloaded, so it
# does not fit a free tier. The cascade reports the stage as absent rather than hiding it,
# and a report with a text layer is still read: 84 of 94 cells on the page measured with
# ground truth, against 94 with the full cascade.
#
# For the full cascade, use requirements-full.txt, or the Dockerfile, on a host with 2 GB.
pandas>=2.2
pdfplumber>=0.11
sdmx1>=2.20
lxml>=5.0
rapidfuzz>=3.9
pydantic>=2.6
pydantic-settings>=2.2
fastapi>=0.110
uvicorn[standard]>=0.29
python-multipart>=0.0.9
requests>=2.31
gradio>=6.0
