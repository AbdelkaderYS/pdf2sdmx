# The full cascade, for a host with 2 GB of memory. Torch alone is 738 MB, so a free tier
# cannot carry it; requirements.txt holds the text stage only and is what a host installs
# by default. Versions match the environment the measurements were made on.
--extra-index-url https://download.pytorch.org/whl/cpu
torch>=2.2
pandas>=2.2
pdfplumber>=0.11
camelot-py[ml]>=2.0
sdmx1>=2.20
lxml>=5.0
rapidfuzz>=3.9
pydantic>=2.6
pydantic-settings>=2.2
fastapi>=0.110
uvicorn[standard]>=0.29
python-multipart>=0.0.9
requests>=2.31
gradio>=6.0
